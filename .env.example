# Ralph Agent Configuration

# API Token (REQUIRED)
# Create this in your Ralph account under API Tokens
# Make sure it has the "ralph_agent" permission
RALPH_API_TOKEN=your_api_token_here

# API URL (optional, defaults to http://localhost:5002)
RALPH_API_URL=http://localhost:5002

# Polling interval in milliseconds (optional, default: 5000)
RALPH_POLL_INTERVAL=5000

# Log level: error, warn, info, debug (optional, default: info)
RALPH_LOG_LEVEL=info

# Maximum retry attempts (optional, default: 3)
RALPH_MAX_RETRIES=3
